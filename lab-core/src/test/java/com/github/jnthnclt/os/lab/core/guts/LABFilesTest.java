package com.github.jnthnclt.os.lab.core.guts;

import com.github.jnthnclt.os.lab.core.guts.LABFiles.AppendedFile;
import java.io.File;
import org.testng.Assert;
import org.testng.annotations.Test;

public class LABFilesTest {
    @Test
    public void testAdd() throws Exception {

//        LABFiles labFiles = new LABFiles();
//
//        Assert.assertTrue(labFiles.isEmpty());
//        labFiles.delete(new File("foo"));
//        Assert.assertFalse(labFiles.isEmpty());
//
//        labFiles.take((appendedFiles, removedFiles) -> {
//            Assert.assertTrue(appendedFiles.isEmpty());
//            Assert.assertTrue(!removedFiles.isEmpty());
//            Assert.assertEquals(removedFiles.size(), 1);
//            return true;
//        });
//
//        Assert.assertTrue(labFiles.isEmpty());
//
//        labFiles.add(new byte[]{1},1234, new File("bar"));
//        Assert.assertFalse(labFiles.isEmpty());
//
//        labFiles.take((appendedFiles, removedFiles) -> {
//            Assert.assertTrue(!appendedFiles.isEmpty());
//            Assert.assertTrue(removedFiles.isEmpty());
//            Assert.assertEquals(appendedFiles.size(), 1);
//
//            AppendedFile remove = appendedFiles.remove();
//            Assert.assertEquals(remove.appendVersion, 1234);
//            return true;
//        });
    }

}